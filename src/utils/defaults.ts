import type { Settings } from "../types/types";

export const DEFAULT_SETTINGS: Settings = {
  color: "red-400",
  font: "kumbh-sans",
  durations: {
    pomodoro: 1,
    shortBreak: 5,
    longBreak: 15,
  },
};
